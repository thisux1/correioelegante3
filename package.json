{
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "all": "npm run dev",
    "test": "npm run test --prefix frontend && npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix backend && npm run lint --prefix my-video",
    "lint:frontend": "npm run lint --prefix frontend",
    "lint:backend": "npm run lint --prefix backend",
    "lint:video": "npm run lint --prefix my-video",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck --prefix backend && npm run typecheck --prefix my-video",
    "build": "npm run build --prefix frontend && npm run build --prefix backend && npm run build --prefix my-video",
    "graphify": "python3 -c \"import sys; sys.exit(0)\" && $(cat graphify-out/.graphify_python) -m graphify ."
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.3.1",
    "cloudinary": "^2.9.0",
    "web-vitals": "^5.1.0"
  },
  "devDependencies": {
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "concurrently": "^9.0.0",
    "ffmpeg-static": "^5.3.0",
    "rollup-plugin-visualizer": "^7.0.1"
  }
}
